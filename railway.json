{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks"
  },
  "deploy": {
    "startCommand": "cd apps/backend && npx prisma migrate deploy && node dist/main",
    "restartPolicyMaxRetries": 5
  }
}
