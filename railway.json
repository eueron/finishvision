{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks"
  },
  "deploy": {
    "startCommand": "npx prisma migrate deploy && node apps/backend/dist/main.js",
    "restartPolicyMaxRetries": 5
  }
}
